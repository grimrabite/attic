---
layout: layout.njk
title: Scenes
---

<h1>Scenes</h1>
<ul>
  {% for post in collections.all %}
    {% if "scene" in post.data.tags %}
      <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
    {% endif %}
  {% endfor %}
</ul>
