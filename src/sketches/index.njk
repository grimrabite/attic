---
layout: layout.njk
title: Sketches
---

<h1>Sketches</h1>
<ul>
  {% for post in collections.all %}
    {% if "sketch" in post.data.tags %}
      <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
    {% endif %}
  {% endfor %}
</ul>
