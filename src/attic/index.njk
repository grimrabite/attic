---
layout: layout.njk
title: Attic
---

<h1>The Attic</h1>
<ul>
  {% for post in collections.all %}
    {% if "attic" in post.data.tags %}
      <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
    {% endif %}
  {% endfor %}
</ul>
